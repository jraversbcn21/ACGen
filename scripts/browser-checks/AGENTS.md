| Auditoría de cierre de sesión | 2026-09-02 | Dos auditores en paralelo (documentación y código muerto) sobre el estado tras las diez PRs del día (#51-#60). **Código muerto: un único hallazgo real, en tres sitios** — el Conversor, la Historia de Usuario y el Refinador pasan `onError: showToast` a `useGenerator`, así que su `gen.error` es `null` para siempre y el `<ErrorBanner message={null} onDismiss={() => {}} />` que conservaron de antes de la migración no pintaba nada; fuera la línea y su import en los tres. Todo lo demás salió limpio y verificado: 442/442 claves i18n con consumidor (las seis `landing.tag.*` se resuelven dinámicamente desde el campo `family` de la tabla de tools), 461 clases de `App.css` sin huérfanas (las cinco candidatas solo aparecían dentro de un comentario que documenta un rediseño anterior), los 19 `Icon.*` con uso, cero ficheros huérfanos y cero exports muertos. **Documentación: cinco afirmaciones falsas, no solo cifras.** (1) `getPrompt()` seguía documentado como "lo llaman los 8 tools en su callback de generación" cuando desde la #60 se llama **una sola vez** en `useGenerator.run()` con `getPrompt(c.view)` — dos menciones corregidas. (2) `getLastBackupAt()`/`markBackupDone()` seguían descritas como API viva de `backup.ts`, borradas en la #58: el recordatorio lee y escribe `acgen_last_backup` directamente vía `useLocalStorage` en `useBackupReminder.ts`; el propio documento se contradecía, porque su tabla de Key files ya no las listaba. (3) El README describía la regla del perfil que la #59 derogó ("un campo vacío se omite del prompt") en vez de la vigente (cae a su valor por defecto). (4) La estructura del README no nombraba `useGenerator.ts` ni `persistence.ts`. (5) La fila de Key files de `scripts/browser-checks/` decía 52 asertos cuando ya son 8 scripts / 109, y la fila de la #60 terminaba en "pendiente la pasada manual antes de mergear" pese a estar mergeada — **el patrón sistemático del proyecto por sexta vez: la fila se escribe con la rama viva y se olvida al mergear**. Las menciones a "52/52 de los 7 browser-checks" dentro del cuerpo de las filas históricas se conservan a propósito: son el registro de lo que se verificó ese día. **Un falso positivo descartado**: el auditor marcó como error aritmético que el total dijera 804 mientras la tabla suma 805; no lo es — la tabla declara 805 casos, vitest ejecuta 804 y salta 1 (el `it.skipIf` del Validador, que no tiene modo confidencial), y el pie ya lo explicaba. Verificación final: 804 tests / 74 ficheros, typecheck limpio, lint 5 warnings (`react-refresh`, sin cambios), build OK, bundle en producción **`index-D_qxcSEu.js`** idéntico al build local, y **109/109 asertos de los 8 browser-checks contra producción**. Cero trabajo colgado: una sola rama, sin stashes, sin PRs abiertas. |
